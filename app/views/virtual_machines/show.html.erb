<p id="notice"><%= notice %></p>

<table class="table">
  <thead>
    <tr>
      <th>VM Name</th>
      <th>Power State</th>
      <th>Disks</th>
    </tr>
  </thead>

	<tbody>
      <tr>
        <td><%= @flatdetails[1]["name"] %></td>
        <td>
          <% if @flatdetails[1]["power_state"] == "POWERED_ON" %>RUNNING<% end %>
          <% if @flatdetails[1]["power_state"] == "POWERED_OFF" %>OFF<% end %>
        </td>
        <td><%= @diskdetails %></td>
      </tr>
      <tr>
        <td colspan="3">
          JSON RESULTS
        </td>
      </tr>
      <tr>
        <td colspan="3"><%= @p %></td>
      </tr>
      <tr>
        <td><%= link_to ('<button [clrLoading]="submitBtnState" type="submit" class="btn btn-success-outline" (click)="submitDemo()">Back to VMs</button>').html_safe, controller: "virtual_machines" %>
        <%= link_to ('<button [clrLoading]="submitBtnState" type="submit" class="btn btn-success-outline" (click)="submitDemo()">Start</button>').html_safe, '/startvm', id: @flatdetails[1]["vm"] %>
        </td>
      </tr>
  	</tbody>

    
</table>
